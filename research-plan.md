# Research Plan & Tracker

*How the Mojo System Interface gets worked out, and the living status of doing
it. Re-derived from [anatomy.md](anatomy.md) on 2026-07-11: the anatomy's
pieces and lettered seams are the organizing structure now, and everything in
here is keyed to them. Distinct from [devlog.md](devlog.md) (the raw, dated
reasoning trail) and `msi.md` (the draft standard itself, which doesn't exist
yet, on purpose). Nothing in this file is true by virtue of being written down.
The Status column is the only thing that means anything, and most of it says
Open on purpose. The prior-art notes carried into the seam rows below were
earned in earlier sessions (see devlog) and are inputs to the walk, not
outcomes of it: a seam walked honestly can overturn any of them.*

---

## The method, taken from how POSIX actually works

POSIX.1 is the model, taken seriously rather than as a slogan:

- **The normative content is interfaces only.** POSIX never defines what a
  kernel or a scheduler is; it defines observable behavior at a boundary, one
  interface page at a time. Nobody conforms to a description of a piece, they
  conform to the contracts the piece sits behind. For the MSI that means:
  anatomy.md's piece prose is informative, the seam contracts are the
  normative spec, and **a piece's real definition is the union of the
  seam-sides it must implement.**
- **Conformance is against profiles, not the whole document.** POSIX has
  option groups; the MSI has ten conformance profiles, each a named bundle
  of seam-sides. §0 gets written at assembly time out of what actually
  landed, never guessed up front.
- **Rationale is non-normative and kept separate.** The why of each decision
  goes in an appendix, distilled from the devlog, not mixed into the
  contracts.
- **It codified existing practice and was drafted iteratively.** POSIX wrote
  down fifteen years of running Unix; OSI wrote its spec first and lost to
  TCP/IP's running code. So: prior art checked before anything is invented,
  sections drafted the session their seam lands, and the assembly pass at the
  end is a coherence read, not a writing project.

## The target: msi.md

**MSI-1** (the first version, the way POSIX-1 was; iterated openly from
there). One file at the repo root until it outgrows that:

- **§0 Conformance.** RFC-2119 language. Ten profiles: Client, Router, Harness,
  Model endpoint, Sandbox, Credential Broker, Provenance, Kernel, Memory
  Provider, Fleet Manager. Each profile lists the seam-sides
  its implementations must satisfy. Written during assembly.
- **§1 Base definitions.** Vocabulary, plus the small set of file-like data
  primitives the identity is built from.
- **§2 The identity schema.** Seam m, the flagship: the data shape, travelling
  permissions, provenance fields, history.
- **§3 Seam contracts.** Every MSI-posture seam, one section each.
- **§4 Adopted-standard bindings.** What adopting the model wire format, MCP,
  SKILL.md, and A2A actually binds a compliant piece to: pinned versions,
  required surface, what the MSI layers on top.
- **Appendix: Rationale.** Non-normative.

## The plan: four phases

**Phase 1 — the piece pass.** Walk the pieces one at a time. For each: get the
anatomy.md prose right (a clear reader should know what the piece is and
isn't), write down exactly which seam-sides its profile owns (the Pieces table
below), and surface what a builder still couldn't answer. Every real gap found
is a seam question and gets recorded in that seam's row, never as more anatomy
prose. A piece that surfaces zero new questions is done; one that surfaces five
just found the real work. If a question has no seam to live in, the anatomy is
missing a seam, and that's a finding, not a filing problem.

**Phase 2 — the seam pass.** Added 2026-07-13: building anatomy.html's map
surfaced a real hole the piece pass never checked for, seam l had no piece
attribution anywhere, not on any piece's owned seam-sides and not covered by
a blanket clause the way j and p each have. Lighter than phase 3: walk the
seams one at a time confirming which piece(s) are actually party to each,
against the Pieces table below, fixing wrong or silently-missing attribution
and flagging real gaps rather than inventing answers to make a diagram
render. Then a dedicated hunt for missing seams entirely, one resync of
anatomy.html at the close, and a holistic stress-test of the whole map
against how real digital-counterpart systems (OpenClaw, Hermes, Letta, Khoj)
are actually architected as whole systems, the same discipline POSIX applied
against real Unix internals, this time at the level of the whole system
rather than one piece or seam at a time. Phase 3 doesn't start until this
holds.

**Phase 3 — the seam walk.** One seam at a time, study-then-decide: open with
a briefing on the seam's named prior art, land the answers, draft the msi.md
section the same session. The old nine-leg walk order is superseded; the
proposed order below is dependency-based and revisable.

**Phase 4 — assembly.** Write §0's profiles out of the landed seam-sides, run
a cross-seam contradiction check, read msi.md end to end once for coherence.
That's MSI-1 done, and [roadmap.md](roadmap.md)'s Next (the first system,
released runnable) starts there.

### Working rules (carried forward, still binding)

- **Timebox rule.** A seam that resists landing after one real session gets a
  provisional answer plus an explicit open-question flag, never an indefinite
  Deciding. MSI-1 is complete-coverage-thin on purpose; depth comes from
  versioned iteration once something runs.
- **Minimal-core rule.** MSI-1's mandatory core for any seam is the smallest
  set of obligations that is actually correct, not the most capable one.
  Anything additive (richer transports, new content types, new capabilities)
  is a named, explicitly deferred extension point, never folded into the
  mandatory core to make MSI-1 look more complete than it is.
- **Definition of done, per seam.** The contract is written well enough that a
  stranger could build a compliant piece against it without ever talking to
  me. Anything less isn't Decided.
- **Draft as you go.** When a seam lands, its msi.md section gets drafted the
  same session, while the reasoning is fresh.
- **Nothing already thought is decided.** The `dockets-research/` files, the
  old plan's leans, and every note carried into the rows below are inputs. A
  seam walked honestly can overturn any of them. Only Decided means decided.

**Phase 1 (piece pass) history.** Client covered
2026-07-11; Peripheral retired 2026-07-12 (collapsed into Tools, no piece,
no profile, no seam); Harness covered 2026-07-13; Model endpoint covered
2026-07-12; Tools covered 2026-07-12; Memory provider covered 2026-07-13;
Sandbox covered 2026-07-13 (also landed a cross-cutting mechanism finding
touching d, e, f, j, q, s: services are granted as rosters, resolved
per-action through the kernel's existing gate, budget enforcement is a real
kernel obligation, not optional); Router covered 2026-07-14 (also landed a
cross-cutting finding touching all of system control: every singular piece
reads the data, only the kernel writes it, folded into anatomy.md's
top-level system-control section rather than stated per-piece; and corrected
vision.md twice, real kernel-like and memory-layer-like implementations
already exist, buried inside monolithic products rather than missing
entirely, and it's not just those two pieces that need a first Mojo-built
reference, it's every piece behind an MSI-posture seam); Kernel covered
2026-07-14 (confirmed bundling contract-issuance, enforcement, and placement
authorization into one piece against Linux kernel/seL4/four
digital-counterpart systems/OpenFang/Kubernetes; fixed a self-referential
ambiguity in the piece's own prose; OpenFang upgraded to a real candidate
for the kernel's first reference implementation; kernel.chat dropped, no
such product exists). Credential broker covered 2026-07-12 (prose fixed, the
piece mediates rather than stores, matching Memory provider's derive-never-own
framing; landed against real custody-at-rest negatives, Vault/STS
scoping-only precedent, and Claude Code's sandbox proxy / OpenClaw's sentinel
mechanism as the real positive precedent for injection-without-holding).
Provenance covered 2026-07-12 (prose confirmed as-is, already origin-based and
mechanism-agnostic; two of the row's three prior-art citations, C2PA and
Sigstore, checked against their real specs and found genuinely mismatched,
media authenticity and supply-chain attestation, not agent content trust,
kept honestly as structural-only precedent rather than silently dropped;
OpenFang's signing confirmed miscited, belongs to seam j; real fits landed,
DeepMind's CaMeL and Anthropic's shipped tool-content classifier as the
working mechanism precedent, OpenClaw's open issue #7707 as the
named-but-unshipped gap matching this project's running pattern; Clarke's
scope call, MSI-1's mandatory core is mechanical origin-based tagging only,
active content classification is a competing richer tier, not invented into
the core). Fleet manager covered 2026-07-12 (prose fixed, cut a restatement
of the top-level system-control singularity rule; checked against OpenClaw,
Khoj, and Letta, Letta's Constellation the load-bearing real counter-example
to the per-machine-instance rule, already settled rather than reopened;
Signal's linked-device model and Tailscale's device approval landed as real
precedent on seam n's row; Apple Continuity/Handoff landed on seam k's row
as real precedent that "who's driving" is a distinct problem from
sync/conflict/partition; real find, seams k and n together are the
cross-implementation-fleet interoperability contract, stakes for a future
Collective, flagged in ideas.md).
Phase 1 closed 2026-07-12 (step 1.13): anatomy.md read end to end for
coherence, clean, no edits needed (no stray peripheral/seam-o references, no
em dashes, seam-letter set and piece count internally consistent); two small
research-plan.md loose ends from the peripheral retirement fixed in passing
(the Closed-comparators precedent's stale "Feed c, o" repointed to f; seam p's
identity-vs-machine-scoped-custody question cross-referenced onto k and n's
rows, it had only lived on p's); anatomy.html brought back in sync with
anatomy.md and given a polish pass.

**Currently on: phase 2 (seam pass) — suspended 2026-07-13.** See devlog.
*(Updated in place as phases and seams complete; the Status
columns stay the real truth.)*

### Proposed seam walk order (phase 3, revisable)

Not a committed sequence: phase 3 now opens with its own planning step
(msi-steps.md 3.1) that looks at whatever anatomy.md phase 2 actually
produced and decides the real order then, on better information than exists
right now. This list is that step's input, a candidate, not its answer.

1. **e, f, h, l (adopted seams).** Cheap, mostly confirmation of what each
   adoption binds us to, and it puts §4 down first. The momentum leg. Flag
   carried: how A2A composes with the federation seam gets revisited at k.
2. **a (owner verification).** Small, and everything else's permissions
   bottom out here; do it before anything that needs "the owner said so."
3. **m (data shape).** The core deliverable and the longest walk. Drafts §1
   and §2. Then **s (memory access)** immediately after: its answer depends
   on m's shape, and its first candidate (collapse into f, providers as MCP
   servers) was already sized up during the adopted-seams leg.
4. **j (enforcement).** Authority as data; the other half of m's travelling
   permissions. Hardest conceptual seam, scheduled right after its
   dependency for exactly that reason.
5. **i (run contract).** Needs m (the fragment's shape) and j (what
   capabilities are handed).
6. **d + g (run assembly, triggers).** The router's two seams; needs i so it
   knows what it's assembling.
7. **p (secret custody), q (sandbox), r (content trust).** Each needs j; none
   needs the others.
8. **c + b (client contract, proactive delivery).** Needs m (what a client is
   handed is fragment-shaped) and g (delivery is the output half of a
   trigger).
9. **k + n (federation, machine admission).** The distributed-systems seams,
   last on purpose: everything single-machine is settled first, and these
   are the hardest rows in the tracker.

---

## Tracker: pieces (phase 1)

The Owner and the identity are listed for coverage but carry no profile: the
owner is a person represented by a credential (seam a's subject), and the
identity is data, not a piece; nothing to conform, only a shape to keep (§1/§2's
subject).

| Piece | Profile | Seam-sides owned | Status | Notes |
|---|---|---|---|---|
| Client | Client | a (owner side passes through it), b (receives delivery), c (its contract) | Covered | Piece pass done 2026-07-11: no session of its own, store-shaped between runs, live only for the duration of a run, needs no AI/harness in the loop to qualify. Checked against real prior art (OpenClaw, Hermes, Letta, Khoj, Matrix, MIME, Kitty) |
| Harness | Harness | e, f, i (run side), q (executes inside), h (reads skills), s (queries providers) | Covered | Piece pass done 2026-07-13: prose tightened (adopted-whole framing, scoped "manages context" to a harness's own live in-run budget), seam-sides confirmed unchanged. Checked against real prior art (OpenAI Agents SDK, Claude Agent SDK, CrewAI, LangGraph, smolagents, OpenClaw's Agent Runtime, Hermes-agent, Letta). Real findings landed on i (no private filesystem, incremental write-back not session-end, subprocess lean, OpenClaw's handoff-object shape), d (trust-tiered selection ties to the existing candidate-transparency flag, OpenClaw's `supports()` predicate as the lean for capability declaration), j (mercenary-tier harnesses are ordinary capability-narrowing, no new mechanism), m (personas confirmed as the mercenary-tier identity mechanism, ideas.md's decoy-cargo sketch carried forward) |
| Model endpoint | Model endpoint | e (serves) | Covered | Piece pass done 2026-07-12: prose broadened to include vendor-hosted endpoints alongside self-hosted (vLLM/Ollama/llama.cpp), trust tier (owned/rented/mercenary) kept invisible to the piece itself, same precedent as Harness's mercenary-tier harnesses (Clarke confirmed the scope call). Checked against real prior art (vLLM, Ollama, llama.cpp current server docs). Real findings landed on e (single-model-per-instance vs. multiplexed-many-models-behind-one-endpoint is a genuine architectural split, vLLM/llama.cpp vs. Ollama, resolved by the wire shape itself, nothing to invent). Renamed from "Model" 2026-07-12, closing the last piece/profile name divergence (same call as Client/Harness) |
| Tools | — (no profile) | f | Covered | Piece pass done 2026-07-12: a Tools instance is one MCP server, exactly parallel to a Model endpoint being one instance; several serve one machine at once (confirmed against Claude Desktop's real `mcpServers` config, which runs several independent servers side by side). Real finding: unlike Model endpoint, a run isn't pointed at one Tools instance, it needs several tool capabilities live at once, so seam f's binding is a roster, a subset of the machine's available servers assembled per run, not a single pick — carried into d and f below. Prose tightened to state this; the retired-Peripheral ground (hardware and software tools are the same MCP shape) re-confirmed, not reopened |
| Memory provider | Memory Provider | m (derives from the data), s (serves runs) | Covered | Piece pass done 2026-07-13: prose rewritten to lead with the front-door framing (a run reads and writes through whichever provider it's handed, never touching the data directly), rest held. Checked against real prior art split into two camps: derive-only (Obsidian + community plugins, Smart Connections/Smart Relations building rebuildable indexes over the canonical vault, the closest real match, confirming the existing Obsidian analogy) versus store-owning (Mem0, Zep, Letta, where the vector/graph DB is the only copy, no separate canonical layer). The dominant real AI-memory-market pattern is camp two, which the derives-never-own rule deliberately rejects; noted as a real, acknowledged divergence, not an oversight. Real findings landed on s (envelope-lock-vs-differentiation resolves the same way f already did, POSIX's VFS operation-table pattern checked as the healthier precedent over ioctl's freeform-arg wart, lean toward a small fixed set of typed operations with mandatory provenance/permission-scoping rather than one opaque call), i (confirms rather than changes the existing incremental-write-back finding), g (consolidation is an ordinary triggered run, no new "overseer" piece, Router already is that; trigger cadence beyond idle-only, e.g. periodic or volume-based, is a router-implementation tuning question, not a new mechanism), and a flag for m's own walk (whether the schema needs to distinguish a raw incremental write from a consolidated/reorganized fact, or whether that's just revision history on the same object) |
| Sandbox | Sandbox | q (provides placement) | Covered | Piece pass done 2026-07-13: prose confirmed as-is (the container/isolated-process/other-machine/remote-service category list holds, "what it can see" stays one clause on purpose, the finer filesystem/network/secret split lives in seam q's contract, not anatomy prose). Checked against real prior art split into general-purpose isolation (Claude Code's own sandbox, the most load-bearing find: Seatbelt/bubblewrap, default-deny filesystem/network scoped separately, credentials injected as sentinels and swapped only at the egress proxy; E2B's always-remote Firecracker microVMs; WASI's capability-handle model; Landlock as a real unprivileged default-deny backend) and digital-counterpart systems (OpenClaw's opt-in mode/scope/backend config plus a real SSH remote-placement backend with its own auth; Letta's no-built-in-sandbox, third-party-provider pattern; Khoj's total absence of execution isolation, a real negative finding; Hermes's Docker-as-whole-boundary as the simplest real tier). Real finding: isolation strength is a genuine gradient (microVM > userspace kernel > container > process isolation), not the binary the category list implies. This piece pass also produced this session's big cross-cutting finding (see d/e/j/q's rows): every service is granted as a roster, resolved per-action through the kernel's existing gate, not a new mechanism, with budget enforcement a real, separately-checked kernel obligation. Open questions recorded in q's row, not resolved here: isolation-strength as a declarable tag, three-way visibility scoping, null/host placement legitimacy, no placement nesting |
| Router | Router | d, g | Covered | Piece pass done 2026-07-14: prose tightened, two restatements of already-established top-level invariants removed (the roster/per-action-kernel-check mechanism, already stated under "Services are plural"; the sub-run recursive-grant fact, already stated under "Runs are ephemeral"); an owner's direct request folded into the trigger list as g's fourth trigger kind, rather than kept as a separate clause; each machine's own router instance now states its specific per-machine reasoning (offline resilience) the way every sibling system-control piece's paragraph already does, with cross-machine trigger coordination named explicitly as the fleet manager's job (seam k), not the router's. Checked against real prior art: Apache Airflow's scheduler (docs confirm one required, always-on component handles both triggering scheduled workflows and submitting tasks to the executor, the same two-jobs-one-piece shape as anatomy's existing Router framing) and systemd (PID 1, watches timer/socket/path/D-Bus triggers, assembles the unit's resource limits and environment on activation), both real precedent that watching triggers and assembling a run are legitimately one piece, not two; Kubernetes' CronJob-controller/scheduler split checked as the real counter-example, confirming the merge is a genuine design choice, not the only option. Checked against real digital-counterpart systems: OpenClaw and Hermes independently converge on the same shape (gateway-local scheduler, persisted job state, tick loop, a separate concurrency lane for triggered runs vs. chat turns), a real cross-project signal for the boring/correct default; Khoj is the real counter-example, a centralized cloud-authoritative scheduler rather than per-device, confirming anatomy's per-machine rule is a deliberate choice against a real alternative, not an oversight; Letta's sleep-time compute turned out structurally uncomparable, memory-consolidation work coupled to the primary agent's lifecycle, not a scheduled trigger with a due time. Real findings landed: (1) system control's read/write asymmetry, every singular piece reads the data, only the kernel writes it, surfaced during this pass but applies to all five system-control pieces, so it's folded into anatomy.md's top-level "System control is singular" section rather than restated per-piece; (2) a genuine ambiguity flagged for seam j's row below, not resolved here; (3) vision.md corrected the same session (see devlog), twice: first, its claim that no implementation of a kernel/memory-layer analog exists was wrong, real enforcement and memory logic already runs inside monolithic products (Claude Code's own sandbox for the former, Mem0/Zep/Letta/Obsidian+community-plugins for the latter, all already checked during earlier piece passes), just never factored out as a swappable piece; second, Clarke corrected the scope further, it isn't just kernel and memory layer that need a first Mojo-built reference, it's every piece behind an MSI-posture (not-Adopt) seam, which the tracker's own Posture column shows is most of them (Router, Sandbox, Credential broker, Provenance, Fleet manager, Client all sit entirely behind MSI seams, same situation as Kernel and Memory provider), because nothing existing can conform to a contract that doesn't exist yet, and those buried-in-monoliths implementations are exactly what all of them get built against |
| Kernel | Kernel | i (hands the run its contract), j, q (authorizes placement) | Covered | Piece pass done 2026-07-14: prose fixed, the self-referential ambiguity flagged during Router's pass ("every consequential action... passes through here," literally read as gating the kernel's own reads/writes) resolved by scoping to "any other piece in the system takes," covering runs, every system-control sibling, and clients writing straight to the data, excluding only the kernel's own bookkeeping, matching what seam j's row already said ("Kernel ↔ everything consequential"). Pieces-table q-side wording tightened from "places actions" to "authorizes placement," matching the piece prose (Sandbox places, Kernel authorizes). Checked whether bundling i (contract) + j (enforcement) + q (placement authorization) into one piece is an honest reading of real systems, same discipline as Router's trigger/assembly check. Confirmed: the Linux kernel does all three continuously as one component (runc/containerd only configure namespaces/cgroups/seccomp once at launch via kernel syscalls; every subsequent syscall is checked in-kernel via LSM hooks and the installed seccomp filter for the process's life; CVE-2024-21626, a runc fd leak exploitable before seccomp took effect, is real evidence of a gap in the kernel's own enforcement window, not a second enforcing component). seL4's capability invocation fuses creation and access-checking (i+j) but has no placement concept at all, a real limit on how far it extends to q. Checked against real digital-counterpart systems: OpenClaw (verified against its own security docs) has a genuine distinct policy-cascade layer (global→provider→agent→session→sandbox) plus exec-approval allowlists, explicit that skills carry no permissions of their own; Hermes has a real gateway/sandbox-host split via SSH ForceCommand, `cap_drop: ALL`, env-var allowlisting at the exec boundary; Letta has RBAC but only at the API-endpoint level, not per-action in-run enforcement, a real partial match; Khoj is a genuine negative, no first-party enforcement layer, its own community discussion flags self-hosted agent security as unsolved. Checked Kubernetes as the real counter-example (scheduler/admission-controllers/kubelet genuinely split into three components), confirmed it's a real working alternative that costs a persistent control-plane (etcd, API server) to stay coherent, a cost single-machine Mojo doesn't pay, so the split is a real tradeoff, not evidence against the bundle. Real find: OpenFang (real repo, real docs) independently converges on this exact bundle under a literal `openfang-kernel` component (contract-issuance, per-action enforcement, budget/placement metering), upgraded from this project's prior "unproven, mechanism ideas only" characterization to a genuine candidate for scaffolding the kernel's first reference implementation. kernel.chat checked and dropped, no such product exists (Clarke's Microsoft Agent Framework lead checked too, no connection found; likely confusion with Microsoft's own Semantic Kernel, unrelated). No new seam-question gaps surfaced beyond what i/j/q's rows already carry |
| Credential broker | Credential Broker | p | Covered | Piece pass done 2026-07-12: prose fixed, the piece does not store secrets itself (Clarke's catch), it mediates between wherever the owner's secrets actually live and the call that needs one, matching Memory provider's already-established derive-never-own framing. "Never sits in a model's context or a harness's hands" widened to include "a sandboxed action's own view," the third real consumer the research surfaced. Checked against real prior art split into custody-at-rest (Linux Secret Service API / gnome-keyring, macOS Keychain Services, sops/age, `pass`: all confirmed to hand plaintext straight back to the requesting process as a normal return value, no injection concept, negative precedent for the piece's actual load-bearing claim) and scoping/expiry (HashiCorp Vault's dynamic secrets, a fresh uniquely-scoped credential minted per lease and auto-revoked; AWS STS `AssumeRole`, a scoped short-lived credential triple with unconditional expiry; both real precedent for "scopes them," neither for "injects them," the caller still holds the live secret). systemd-creds checked as a real middle case, kernel-enforced per-access check, not propagated to child processes the way env vars are, but still no per-call scoping. The load-bearing positive precedent: Claude Code's own sandbox proxy and OpenClaw's sentinel mechanism independently converge on the same shape, the consumer only ever holds an opaque placeholder, a component outside its own memory swaps in the real value only at the network/adapter boundary right as the call leaves; OpenClaw's own docs honestly flag theirs as same-process protection only, a real, weaker guarantee than Claude Code's cross-process sandbox boundary. Letta adds a third variant, redaction in both directions (substituted in, scrubbed from output on the way back) with secrets bound to agent identity rather than machine. Real negatives, matching this project's running pattern: Hermes keeps secrets as plaintext in `~/.hermes/.env` with full-environment inheritance by every subprocess, a self-admitted gap on its own tracker; Khoj has no broker concept at all, plain env vars, consistent with its total absence of every other system-control piece found in earlier passes. Two questions recorded in seam p's row for its real walk, not resolved here: whether injection strength needs a declarable tier (same-process sentinel-swap vs. cross-process boundary-swap), parallel to seam q's already-open isolation-strength question; and whether Letta's identity-bound-not-machine-bound secrets mean broker custody should be identity-scoped rather than machine-scoped in a fleet, ties to k/n |
| Provenance | Provenance | r | Covered | Piece pass done 2026-07-12: prose confirmed as-is, already origin-based and mechanism-agnostic ("where it came from... whether it arrived from the open web"), no edit needed. Checked real prior art and found two of the row's three existing citations were mismatched: C2PA (verified against the actual spec) tags media authenticity at creation time, is this image AI-generated, not whether an agent should trust ingested text, no assertion is even mandatory so a malicious source simply ships with none; Sigstore/SLSA is software-supply-chain attestation (this build came from this commit), not content trust. Both real, shipped standards, kept in the row as structural precedent (signed claim + transparency record) only, not as this seam's working mechanism, an honest mismatch rather than a silent drop, Clarke's call. OpenFang's Ed25519 signing also checked and confirmed as a genuine miscite, it signs capability/identity manifests, already correctly credited to seam j's row, not content. Real fits found: Anthropic's own shipped tool-containment mechanism (a classifier inspects tool return values before they enter context, real and current); DeepMind's CaMeL (2025), a taint-tracking interpreter attaching unforgeable origin tags to data as it flows through, gated at sensitive sinks, the closest working precedent for tag-here/enforce-at-kernel as two cooperating pieces, matching this project's own split; OpenAI's Instruction Hierarchy, real and shipped as message-role privilege, confirmed necessary-but-insufficient alone, it can't distinguish a web result from the owner's own file if both arrive under the same role. OpenClaw issue #7707 ("Memory Trust Tagging by Source," open, unimplemented) names this exact gap by source-tier and decay policy, real evidence the digital counterparts recognize the problem without having shipped the piece, consistent with this project's running pattern. Hermes, Letta, Khoj: no mechanism found, consistent negative. Clarke's scope call, minimal-core rule applied: MSI-1's mandatory core is origin-based tagging at the point content crosses in (mechanical, tied to which fetch/tool path it came through), not a mandated content classifier, active inspection (Anthropic's shape) is a legitimate richer tier implementations can compete on, not invented into the mandatory core. Two questions parked for seam r's real walk (step 2.14), not resolved here |
| Fleet manager | Fleet Manager | k, n | Covered | Piece pass done 2026-07-12: prose fixed, cut a restatement of the top-level system-control singularity rule already stated once at the document's top (the "two fleet managers disagreeing about who is in the fleet" clause duplicated that section's own "who is in the fleet" example verbatim), replaced with the same offline-resilience reasoning Router's and Kernel's paragraphs already give for the identical one-instance-per-machine fact. Checked against real digital-counterpart systems: OpenClaw has no fleet concept at all, two open, unshipped feature requests confirm the gap directly (#47871 node awareness, #38878 multi-device shared memory); Khoj is a centralized hub, one server, many thin clients, not peer machines, consistent with its running role as the centralized-cloud-authoritative counter-example; Letta's Remote Environments is the load-bearing positive, a real product where the same agent moves mid-conversation between machines and its memory follows, but brokered through Constellation, Letta's own cloud coordination service, real precedent that a coordinating-hub shape works and a real counter-example to anatomy's per-machine-instance rule, the same role Khoj played for the router, not a reason to reopen that rule (already settled during Router's 2026-07-14 pass). Distinguishing the rule from the strategy: what's foreclosed is an external, cloud-authoritative single point the way Constellation is; a leader-election or primary-designation strategy run among the owner's own already-live per-machine instances (Signal's primary-device-among-peers is the closer real precedent) stays open to implementations as an internal coordination choice. Adjacent real standards checked for the seam-side mechanics: Signal's linked-device model (a primary device is mandatory, others join by scanning its QR code, owner-authenticated admission, no third party approves; revocation induces a key ratchet and a newly-linked device does not inherit past history, admission and revocation are not symmetric operations); Tailscale's device approval (an admin approves a new node; deauthorizing and deleting are two distinct, differently-final actions; node-key revocation is near-instant); Apple Continuity/Handoff as the closest real precedent for "who's driving" specifically, a lightweight activity-description signal kept architecturally separate from the bulk data-sync mechanism (iCloud), real evidence it's not the same kind of problem as sync/conflict/partition even though the piece's own prose names them in one breath. Two open questions recorded on seam n's row for its real walk, not resolved here: who authorizes a new machine's admission, and whether an admitted machine receives the full data immediately or a scoped subset. Real find: seams k and n together are also the cross-implementation-fleet interoperability contract, a machine running one compliant fleet-manager implementation joining a fleet whose sibling machines run a different one, real stakes for a future Collective/Armada made of independently-built fleets, not pursued now, flagged in ideas.md. |

**Retired 2026-07-12 (piece pass, 1.2): Peripheral.** Not a piece, no profile,
no seam. Two independent real systems checked (OpenClaw's computer-use/browser
control, Hermes-iOS's camera/mic/health/location/notifications) both route
device capability through the same general tool-calling path as everything
else, one MCP server, no separate contract. The kernel already gates every
consequential action regardless of piece (seam j), so seam o had nothing left
to contract once declaration and invocation-checking are already covered by f
and j. Continuous sensor telemetry (location history, health metrics) isn't
even a live call, it's data landing in storage and queried later, m/s's
concern, not a peripheral one. Ten pieces, ten profiles, ten seams now. Prior
art gathered for the retired seam folded forward: Matter/HomeKit's typed
capability declaration ("clusters"/"characteristics") noted on f as the
richer schema shape to check MCP's tool schemas against; iOS/Android's
static-manifest-plus-runtime-prompt split and the W3C Permissions API's dead
`revoke()` noted on j as prior art for capability risk tiers and the
revocation question.

**Resolved 2026-07-11 (was a flag: the harness ↔ memory provider binding had
no seam letter):** it's seam **s** now, in anatomy.md and the tracker below.
The router picks providers per run, so a harness has to be able to talk to
whichever provider it is handed; without a uniform surface that's a
harness×provider compatibility matrix, the exact thing the standard exists to
prevent. Same reasoning that gave models seam e and tools seam f. Whether the
walk lands an MSI-defined envelope or collapses s into f (providers as MCP
servers) is the walk's call; the seam exists either way.

## Tracker: seams (phases 2 and 3)

Status: **Open** (not yet walked), **Deciding** (actively being worked),
**Decided** (landed, drafted into msi.md, reasoning recorded). Phase 2's seam
pass confirms attribution and prose, flags gaps; it doesn't move a row past
Open on its own. Only phase 3's real walk moves a row to Deciding or Decided.
Posture from
anatomy.md: **MSI** (the standard's own work), **Adopt** (existing standard,
consumed), plus anatomy's third posture, deliberately-not-standardized, which
shows up here as explicit leave-open notes inside rows rather than whole rows.

| Seam | Connects | Posture | Status | Prior art carried forward, and the open questions |
|---|---|---|---|---|
| a | Owner ↔ any client | MSI | Open | The login of the system: how a key-pair/DID-shaped credential proves "the owner said so." Prior art: what a Unix uid mechanically is (referenced by files and processes, itself neither); Plan 9's per-namespace identity; W3C DID/VC specs (unverified); SAIHM's seed-derived post-quantum keypair that never leaves the holder's machine (worked instance, not adopting the protocol, devlog 2026-07-10). Open: root of trust, where does credential #1 come from at install; where keys live day to day; groups/Collectives deferred past MSI-1 (gated) |
| b | System → owner | MSI | Open | That a compliant client can receive an unsolicited delivery; urgency policy is the owner's data. Prior art: ntfy/push conventions; Matrix push gateways; plain email as the degenerate case. The output half of g: a trigger firing is only useful if the result reaches the owner somewhere |
| c | Client ↔ system | MSI | Open | What a client is handed, may cache, must never hold authoritatively; a client holds no session of its own, reads/writes against the identity between runs, live only for the duration of a run addressed to it; includes how a client is pointed at the right one of the owner's machines when more than one exists (folds in here, not a separate seam). MSI-1 scope, minimal-core rule applied: core covers discrete request/response plus async delivery only; real-time continuous-stream clients (voice, phone, eventually video) are a named, explicitly deferred capability extension, not designed in MSI-1. Prior art, checked 2026-07-11: Matrix's device model + `/sync` (one account, many device_ids, clients reconcile against server state, not a permanent push session, confirms the store-shaped reading); MIME `multipart/alternative` (content offered in several representations, a client renders whichever it understands, no central registry of client types needed); Kitty's graphics-protocol query/response as an alternative capability-negotiation shape (client probes, terminal replies only if supported, silence means no); OpenClaw's real architecture (Gateway resolves sessions and dispatches to an ephemeral per-message Agent Runtime, channel adapters hold no state of their own, close match to this seam's shape); Letta's agent/conversation/session split (durable memory server-side on the agent, a session is a disposable client-side connection holding only transient turn state, closest match found); Khoj (self-hosted mode is structurally close to Mojo's own single-machine shape; its Obsidian plugin is real precedent for a non-conversational, direct-identity-editing client). Open question surfaced by OpenClaw: its Command Queue serializes messages per session to prevent state corruption when multiple inputs target one in-progress conversation; c has no answer yet for what happens when two clients address the same live run at once. Flagged 2026-07-12 (Client piece pass, prompted by the borrowed/foreign-hardware case: a friend's laptop, a work machine): Clarke's lean is that caching may not be worth allowing at all, not just scoped down, since a device the owner doesn't own is exactly where cached state is most exposed; not decided, real candidate answer for c's real walk alongside the two-clients-one-run question above |
| d | Task → a run's assembly | MSI | Open | Inputs (task, roster, constraints, budget) and output (a full piece-set, chosen again for every sub-run); selection quality is competitive, hard limits are the owner's policy data, never router properties. Prior art: Slurm's resource-matching concepts (reference, not adoption); model-router products; the closed-vendor pass found nothing resembling a task-aware router (devlog 2026-07-10). Scheduling/priority under contention parked here (AIOS's context-interrupt, snapshot/resume mid-generation, is real working precedent when this comes up). Doing the selection call by hand first; a trained router is implementation, not contract. Flag carried, 2026-07-11 (outside review): authority already sits with the kernel, not the router (anatomy.md's existing split holds), but a router with no formal authority can still become a de facto black box if its selection isn't inspectable; the contract should require a router's decision to expose which candidates existed and why one was chosen, and stay overridable by the owner's policy data, not just correct. Finding, 2026-07-13 (Harness piece pass): trust-tiered harness selection (a chartered open-source runtime vs. a scoped-down mercenary-tier one; real precedent, ideas.md's decoy-cargo/session-proxy sketch, named against Claude Code/Codex specifically) is a concrete real case of this same selection-transparency obligation, not a new one; to every piece downstream of the choice a mercenary-tier harness looks like an ordinary harness, only d (selection) and j (capability grant) know the difference, and router+kernel jointly own enforcement across every trust axis (model, harness, and whatever comes later), scoped to the degree the owner's policy wants. Open question, not yet answered: how a harness advertises optional capabilities (e.g. guaranteed structured output, sub-agent spawning, live streaming) so the router can pick among candidates without guessing; no real SDK checked (OpenAI Agents SDK, Claude Agent SDK, CrewAI, LangGraph, smolagents) has a third-party-consumable format for this, each just documents its own flags, and the capability set itself will keep changing, arguing against a fixed enumeration if MSI-1 answers this at all. Finding, 2026-07-12 (Model endpoint piece pass): Clarke flagged a live question this row doesn't answer yet, whether per-run model selection is one shot (a single model handed at assembly, fixed for the run) or can hand a run more than one endpoint of the *same* modality to choose among per step (a cheap model for routine steps, a stronger one for a hard one) — distinct from the already-settled cross-modality case (chat vs. embedding vs. image vs. speech, which the Model endpoint piece prose already covers as separately-swappable endpoints). Left open for d's real walk. Either answer makes it a router+kernel job (assembly grants the set, enforcement checks each call against it), harder than single-model assembly but the same shape as the sub-run-gets-its-own-selection recursion already in this row, not a new mechanism class. Two real candidate shapes surfaced: adopt MCP's own already-proven two-layer pattern (a static manifest for cheap pre-launch selection, a live initialize-style handshake at invocation to confirm), or defer the whole mechanism per the minimal-core/timebox rules, router selection in MSI-1 stays coarse (whichever harness is configured), richer matching waits for real implementations to learn from. Research reported (2026-07-13, single pass, see verification note below): OpenClaw is real, already-shipped precedent for something like this, each harness reportedly implements a `supports(context) => {supported, priority} | {supported: false}` predicate, core reportedly calling it during selection (explicit model/provider config wins first, then `supports()`across registered harnesses, then a fallback), with debug logging reportedly exposing the chosen harness, the selection reason, and every candidate's result, the exact selection-transparency shape this row already wanted, if accurate. Only one actual declared capability flag was reported anywhere checked (`authBootstrap: "harness"`, said to be added ad hoc for one integration's real need). Verification note (2026-07-13): a second, deeper pass independently confirmed the repo (github.com/openclaw/openclaw), its MIT license, and, via a real GitHub issue, that it genuinely swaps between independent real backends (an embedded default runtime, OpenAI's Codex CLI, Anthropic's Claude CLI, ACP) — that general finding is solid, doubly sourced. It could NOT independently locate the`supports()` interface, the exact priority order, or the `authBootstrap` flag in actual source; those specifics are single-sourced from web content, not verified code, and should be treated as a lead, not a fact, until someone actually clones the repo and greps it. Letta separately deprecated its own declarative `tool_rules`system in 2026 explicitly to avoid inhibiting frontier capabilities, a second independent real signal (unrelated to the OpenClaw specifics above) against upfront declarative capability machinery. Lean, not yet Decided, resting on the confirmed general shape rather than the unverified specifics: MSI-1 adopts something like OpenClaw's approach rather than inventing a schema, a context-dependent predicate per harness, plus making candidate-exposure and selection-reasoning a required part of d's contract instead of optional debug output; no capability enumeration needed since it's a predicate, not a manifest, and something like it is already proven in production rather than purely speculative. Finding, 2026-07-13 (Sandbox piece pass, cross-cutting): the harness-granularity question generalizes past model selection to every service a run touches. Real prior art (seL4 capability invocation, a local kernel-table check with an explicit fast-path optimization against paying a round-trip cost; AWS IAM/STS, a session's policy evaluated locally per call against the credential it's attached to, not a live round-trip to a separate authoriser; MCP's own already-adopted session model, capability negotiation once, then cheap per-call dispatch to whichever open session owns the tool) converges on one answer: a run is granted a SET of instances per service at assembly (a roster, generalizing f's already-found roster shape past Tools to every service), and which member an action actually uses is resolved live, per-action, through j's existing per-action mediation, not a second round-trip back to the router. This resolves this row's own open question: per-run model selection isn't one-shot, a run can be handed several endpoints of the same modality and the harness picks per step, the same mechanism, no new machinery needed. Checked against Kubernetes admission webhooks as the real counter-example (a live round-trip before every request): documented as a known operational cost, not a default pattern, confirming the round-trip alternative loses on its own terms. Checked against real digital-counterpart systems too: Hermes ships exactly this (main model plus 11 independently-overridable slots, each with its own fallback chain, swappable mid-session with memory intact) including fully automatic swaps via fallback/credential rotation; OpenClaw supports it via explicit rebinding (`/codex bind`, ACP handoff) rather than continuous per-turn resolution; Khoj is the real negative case, one model fixed per agent, "switching" means picking a different agent, which is this same architecture's sub-run pattern, not roster resolution. Real cost surfaced, not to be hidden: Hermes documents that a mid-session model swap resets the prompt cache, a genuine tax the contract must name rather than assume swapping is free. Candidate finding, 2026-07-13 (discussing persona scoping, not yet checked against real prior art): persona selection may need to resolve *before* the rest of the roster rather than sitting as a peer pick alongside model/harness/sandbox, since a persona's scope could plausibly gate which tools or models are even eligible (a work persona barred from a personal tool, or carrying a different budget). Not decided, a candidate for this seam's real walk |
| e | Harness ↔ model endpoint | Adopt + envelope | Open | OpenAI-compatible baseline; how optional capabilities (structured output, reasoning traces) are declared rather than assumed; endpoints modality-typed (chat, embedding, image, speech), never assumed chat-only. Walk records the pinned surface. Finding, 2026-07-12 (Model endpoint piece pass): checked vLLM, Ollama, and llama.cpp's current server docs against the modality-typed-endpoints claim, confirmed accurate (llama.cpp's own server lists them as distinct paths, matching OpenAI's real shape). Surfaced a real architectural split not previously in this row: vLLM and llama.cpp run one model per server instance (multi-model means separate processes behind a proxy, confirmed on vLLM's own docs and forums), Ollama runs one server multiplexing many models, swapped per request via a `keep_alive`-governed load. Resolved by the wire shape itself, not a new mechanism: `model` is always a per-request field regardless of which shape sits behind the endpoint, and `/v1/models` (real, standard, implemented by all three) already answers discovery; the walk doesn't need to invent either. Also confirmed, same session: the Model endpoint piece's scope includes vendor-hosted frontier APIs (Claude, GPT) as ordinary, capability-narrowed instances, invisible to the piece itself, the same pattern as d/j's mercenary-tier-harness finding — this is why step 2.1 already planned to read the Anthropic Messages API alongside the OpenAI-compatible surface, and matches vision.md's existing "hired help, minimum fragment" framing of frontier models (vision.md:154-160). Finding, 2026-07-13 (Sandbox piece pass, cross-cutting): confirms d's roster finding from this seam's side, and names a real cost honestly rather than hiding it, Hermes's documented "mid-session switches reset the prompt cache" is the concrete instance of the general swap-cost caveat; model endpoint is the clearest case where swapping isn't free, because the model's own context/cache state doesn't travel with the swap. The contract should require declaring this cost, not assume roster resolution is uniformly cheap across every service |
| f | Harness ↔ tools | Adopt | Open | MCP, pinned version; enforcement stays with the kernel, not the protocol. Tool discovery is MCP's own listing (the old PATH-equivalent question dissolves here unless the walk proves otherwise). Carries the retired Peripheral piece's ground: device/hardware capability (camera, location, appliances) is just a tool, checked against two real systems (OpenClaw's computer-use path, Hermes-iOS's camera/mic/health/location/notifications all served from one MCP server, devlog 2026-07-12). Matter/HomeKit's typed capability declaration ("clusters"/"characteristics") is the richer schema shape to check MCP tool schemas against for device-facing tools. Finding, 2026-07-12 (Tools piece pass): a Tools instance is one MCP server (confirmed against Claude Desktop's real `mcpServers` config, several independent servers run side by side on one machine); a run's binding to f is a roster, not a single pick, a harness needs several tool capabilities live at once, unlike a model or sandbox binding. Open question for the real walk: how the router assembles that roster and how f's contract exposes it to the harness. Finding, 2026-07-13 (Sandbox piece pass): the roster shape found here (a run gets a set, not a single pick) turns out to be the general answer, not a Tools-specific one; every service seam (d, e, j, q, s) now carries the same mechanism, resolved per-action through j's existing gate rather than a new one |
| g | A trigger → a run | MSI | Open | How a run fires with nothing else running: the router watches the clock, outside events, idle time; downtime maintenance is just a run; triggers themselves are data (m's reused-shape catalog verifies that). Prior art: cron/at; systemd timers; iCalendar RRULE as the recurrence grammar; Erlang timers; Letta's sleep-time compute, checked 2026-07-11 (memory reorganized and consolidated during idle periods by a background run, not a continuously-alive foreground agent) as real precedent that downtime-maintenance-as-a-run is the right shape for continuity, not a reason to keep a runtime alive between engagements. Finding, 2026-07-13 (Memory provider piece pass): a deep memory-consolidation pass doesn't need, and shouldn't get, a dedicated always-on "overseer" piece; it's an ordinary triggered run, Router already is the overseer, no new mechanism. "Downtime" as the only trigger for it is too narrow, live-tier learning (seam i's existing incremental write-back) already covers in-session capture; the deep pass just needs to fire often enough to stay useful, which is cadence tuning (idle, periodic, event-count-based) left to the router implementation, not an MSI mandate. Finding, 2026-07-14 (Router piece pass): checked Apache Airflow's scheduler and systemd's timer/socket/path activation as real precedent that trigger-watching and run-assembly are legitimately one piece; systemd's `Persistent=` option and OpenClaw's own issue tracker (documented, real, repeated missed-trigger and stale-lock failures traced to trigger state living only in the running process, not in durable storage) both point the same direction: whether a missed trigger fires late on restart has to be recoverable from durable, data-shaped trigger state, not from the router process's own memory, or restart behavior stops being something any compliant router gets right the same way twice. Open question for g's real walk, not yet answered: does a trigger's record need an explicit owner-set catch-up policy (systemd's `Persistent=true` is the real precedent) alongside its recurrence definition, or is silent-drop-on-restart an acceptable default. Also confirmed the same session: an owner's direct request is g's fourth trigger kind, alongside clock/outside-event/idle, not a separate mechanism, closing what was previously a separate clause in the Router piece's own prose |
| h | Skills → harness | Adopt | Open | SKILL.md as the on-disk shape (agentskills.io); skills live in the identity and travel with it, read straight off the data, never a per-run binding. Walk records what adoption binds. Finding, 2026-07-13 (discussing persona scoping): whether a given run can see a given skill is not this seam's question at all, it's an ordinary instance of m's per-unit permissions cutting a scoped view, enforced the same way any other memory read is; h stays narrow, the adopted format for what crosses, full stop |
| i | Kernel → harness | MSI | Open | The run contract: what a run is handed (identity fragment, capabilities, task), what it owes back (learnings, in the data's shape), how a live run is interrupted or killed. Prior art: exec/argv/env as the analogy; Erlang/OTP supervision and let-it-crash (likely the real process-model precedent, not Unix or seL4); Agent libOS's AgentProcess (state + children + budgets + checkpoints + explicit capabilities) and checkpoint fork/restore/commit (thin evaluation, one author). Folded in from the old plan: the fragment's shape is m's business, who may mint one is j's; uniform I/O; supervision vs. the run's own error/retry (where that line sits is the open question); write-back obligation (incremental vs. session-end still open). Finding, 2026-07-11 (Client piece pass): the feeling of continuity across a gap, knowing what was just being discussed, is a memory-quality problem, not a reason to keep a run alive between engagements; a run's starting fragment must be able to carry enough recent context (recent logs, standing summaries) for this to work, what counts as enough and how it's assembled is m/s's business, but i's contract has to guarantee the fragment can carry it. Explicit leave-opens to record: planning strategy, and a harness's own live in-run context-budget management — harness internals, the market's problem. Finding, 2026-07-13 (Harness piece pass): a harness holds no private filesystem or staged copy of the identity; it reads and writes against the shared data directly (through whichever memory provider it is handed, seam s) throughout a run, the same way every other consumer does. This resolves the incremental-vs-session-end question above: incremental, not batched at the end, a private copy merged at run-end would just reintroduce the concurrent-write-reconciliation problem m's design (concurrent writes reconcile at the data, never inside one provider) already exists to prevent. Real precedent checked (OpenAI Agents SDK's `Runner.run()`, Claude Agent SDK's `query()`, LangGraph) invokes a harness as an in-process library call, not a subprocess or network service; Clarke's lean runs against that grain on purpose, a run should be its own OS-level process, matching the ephemeral/killable claim and the exec/argv/env analogy already carried here, though it means adopting a vendor SDK as a Harness costs a subprocess wrapper. Not yet Decided, a steer for the walk. A process boundary buys killability and crash isolation only, never the security boundary, that stays j's regardless. Real precedent for the handoff object's contents (2026-07-13, from OpenClaw): before a harness runs, core resolves everything into one object, provider/model, auth, context budget, transcript, workspace/sandbox/tool policy, streaming callbacks, worth checking against when i is walked |
| j | Kernel ↔ everything consequential | MSI | Open | The permission model: granting, narrowing, revoking, auditing, offline operation. Direction picked (not designed): seL4-flavored capabilities, not Unix ambient DAC (the documented road not taken). Prior art: seL4's Capability Derivation Tree + Revoke (subtree revocation, the candidate mechanism, check whether Capsicum's no-revoke fd model is actually sufficient instead); WASI's handed-capabilities model — the portable direction, no ambient access, what you weren't given isn't there; Capsicum (FreeBSD-only, so shape-precedent, never the mechanism); Landlock/macOS sandbox as per-OS hardening backends underneath, never the contract; UCAN/Biscuit/macaroons as the portable policy-token family (must round-trip with m's travelling permissions); AgenticOS's capability + intent-check as complementary layers, and unreachable-by-construction rather than runtime-gated; OpenFang's capability gates as a source-level check, kernel.chat dropped from this row 2026-07-14 (checked, no such product found). Audit trail folded in: what the kernel records per mediation, records presumably memory-shaped (m verifies); "what did it do and who allowed it" is a vision.md commitment. Carries the retired Peripheral piece's ground (devlog 2026-07-12): iOS's Info.plist and Android's manifest both declare capability access statically and gate the risky ones with a runtime prompt, Android explicit about tiering ("dangerous" vs. "normal") in a way j's model doesn't yet; the W3C Permissions API's `revoke()`, specified then pulled, is a real shipped standard that tried programmatic revocation and failed, worth checking against before assuming CDT+Revoke solves it cleanly. Finding, 2026-07-13 (Harness piece pass): mercenary-tier harnesses are a concrete real case of this seam's capability-narrowing machinery already, not a new mechanism, the kernel simply grants a narrower capability set by construction; the harness itself never knows it's mercenary-tier, an ordinary harness from its own seat, the distinction lives entirely at d (selection) and j (grant). Finding, 2026-07-13 (Sandbox piece pass, cross-cutting, the load-bearing one): j's per-action mediation is the mechanism that makes every service a roster instead of a single per-run pick viable at all, confirmed against real capability systems, not invented. seL4's capability invocation is a local kernel-table check against a capability the caller already holds, no external decision-maker consulted, and seL4 explicitly built an IPC fast path because avoiding a round-trip on this exact hot path was a top design priority (Liedtke, "IPC performance is the master"). AWS IAM/STS session policies work the same way, a policy document attached to the credential once, evaluated locally per API call thereafter. The rejected alternative, a harness asking a live decision-maker before every single action, is real (Kubernetes admission webhooks) and is documented as a known operational liability precisely because it sits synchronously in the critical path, reached for only when a decision genuinely can't be precomputed, never a default. Second finding, budget: capability tags (what an instance can do) are pure selection-layer metadata, the router/harness reads them to optimize, j never needs to understand them. Cost tags (what an instance consumes) are different, budget is already owner policy data and a hard limit the kernel obeys, so metering spend against that limit is j's job, not the picker's. Checked against real systems: OpenRouter and LiteLLM both keep budget enforcement as a structurally separate, hard-blocking mechanism from capability-based routing (OpenRouter's `limit_usd` returns a hard 402, LiteLLM has a named "Agent budgets" feature, both distinct config surfaces from model/provider selection). Hermes enforces a hard iteration budget the same separated way but has no dollar-cost enforcement at all, a documented real gap. OpenClaw has no built-in spend cap whatsoever, pushes it onto the provider's own console, and has a documented real incident, a runaway retry loop burning $200 in one session before anyone noticed. Every system that skipped hard budget enforcement got burned for it; this argues budget enforcement belongs in MSI-1's kernel obligations, not left optional. Flag, 2026-07-14 (Router piece pass): a real ambiguity in Kernel's own anatomy prose surfaced while confirming that all system-control pieces read the data and only the kernel writes it. Kernel's own line ("every consequential action... passes through here") reads literally as covering Kernel's own reads and writes of the data it enforces, which can't be right, a checker doesn't ask itself for permission to read its own tables. Whether that line means run-initiated actions only, or needs an explicit carve-out, is unresolved and left for j's real walk, not decided here |
| k | Machine ↔ machine | MSI | Open | One identity across N machines: ordering, conflict, partition, live-task handoff (who's driving). Prior art: Plan 9's 9P for addressing something remote through the same namespace as something local (load-bearing); Erlang distribution; capability handoff over the wire (Unix fd-passing, seL4 IPC transfer, Zircon channels passing capabilities in messages, Mach ports, UCAN-style delegation — all unverified); transport reuses Tailscale/WireGuard underneath, seam-only, nothing persists there. Fleet coherence is the hardest row in this file: git (explicit merge, human-visible) vs. CRDTs (Automerge/Yjs, automatic convergence) are opposite answers to the same question, and the semantic-merge problem (two branches of belief state that both claim true and contradict) sits under both. Revisits the A2A-composition flag from l. Concrete instance for the walk, from Router's 2026-07-14 piece pass: two machines' routers reacting to the same fired trigger (a shared calendar event, an idle-time consolidation window) is explicitly this seam's problem, not the router's own, since a router is per-machine by the same singular-system-control rule kernel and fleet manager already follow. Finding, 2026-07-12 (Fleet manager piece pass): Letta's Remote Environments/Constellation is real product precedent that live-task handoff itself works (the same agent moves machines mid-conversation, memory follows), but Constellation is Letta's own external cloud coordination service, a real counter-example to anatomy's per-machine-instance rule rather than evidence against it, the same role Khoj played for the router (already settled, not reopened). What that rule forecloses is the external-cloud-authoritative shape specifically; a leader-election or primary-designation strategy run among the owner's own already-live per-machine instances stays open to implementations, Signal's primary-device-among-peers being the closer real precedent than Constellation. Apple Continuity/Handoff is real precedent that "who's driving" is architecturally distinct from sync/conflict/partition: a lightweight activity-handoff signal, kept separate from the bulk data-sync mechanism (iCloud). Cross-referenced from p (2026-07-12): whether broker custody should be identity-scoped rather than machine-scoped in a fleet is an open question for this seam too, not just n's |
| l | System ↔ outside agents | Adopt | Open | A2A, pinned version; different owners, opaque, no trust extended. The one real question: how it composes with k (machine-to-machine inside one fleet is capability-mediated; agent-to-agent across owners extends nothing). Flagged 2026-07-13: unlike every other seam, l has zero piece attribution anywhere in the Pieces table above, not itemized on any piece's row and not covered by a blanket clause the way j and p each have; anatomy.html's map guessed Harness by analogy to e/f (Harness is the piece that would actually make the A2A call) but that's not decided, it's the reason phase 2 (seam pass) exists, step 2.12 decides this for real |
| m | Providers ↔ the data | MSI | Open | The flagship seam: the standard data shape, everything derived rebuildable from the data alone, history so a bad write rolls back, per-unit permissions travelling in the data (they are what cuts a persona's view), concurrent writes reconciling at the data, never inside one provider. Carried from the old File-side rows: the core file-like primitives (identity, types, naming, namespace; Plan 9's per-process namespaces the load-bearing namespace precedent; does a unit ever have more than one name); content mutation over time — retraction (cheap, common, keeps history) vs. purge (rare, deliberate, actual erasure), with git's object model (immutable objects + moving refs) and Plan 9's Venti (content-addressed but single-timeline, no branches) as the two real precedents; concurrent same-machine writes. Carried from the old schema rows: the memory unit's shape (Portable Agent Memory, Engram, W3C AI Agent Memory Interop CG — checked adapt-before-build, all unverified); persona format (nothing standardized found); policy shape (UCAN/Biscuit/macaroons/Cedar, the enforcement semantics being j's half); budgets; provenance fields (shared with r); the scoped-view fragment (what a run or an outside model is handed, minimum content, stand-ins for real values); write-back landing in the shared shape; and the reused-shape catalog — verify per object, never assert, that a trigger, a routing policy, the roster, a task's checkpointed state, and an audit record are all just memory objects. Retrieval/embedding engines deliberately leave-open (providers compete); the harness↔provider binding question is flagged in the Pieces table. `dockets-research/*.md` are pre-anatomy input material for this walk, nothing more. Flagged 2026-07-11: whether the schema should name three sub-shapes explicitly (data about the owner; the counterpart's own operational identity, its own accounts, credentials, skills, and operational history; and the relationship/provenance data bridging the two) rather than the current undifferentiated list. Credential broker and kernel already don't care whose name is on an account or secret, only owner policy gates action, so this reads as a schema-naming question to settle when m is walked, not a new mechanism. Finding, 2026-07-13 (Harness piece pass): personas (already defined, a scoped view over the same memory cut by per-unit permissions) are confirmed as the mechanism for a mercenary-tier harness's faked/proxied identity access, no new schema needed. ideas.md already has a working sketch worth carrying into the walk: decoy data served per-object as a low-trust harness reads it, translated back onto the real object on write, proxied for the whole session length rather than per-prompt, named explicitly against Claude Code/Codex as the motivating case. Flagged 2026-07-13 (Memory provider piece pass): whether the schema needs to distinguish a raw incremental write (logged live, in-run) from a consolidated/reorganized fact (produced by a deferred deep-consolidation run, seam g), or whether that's just revision history on the same object, git's object model precedent (already named above) the likely answer but not yet checked against this specific case. Finding, 2026-07-13 (discussing persona scoping): creating a persona or changing what it can see is not a new mechanism, it's an ordinary owner-authorized (a) permission write, and only the kernel writes those (j); this row's per-unit-permissions answer already covers it. Reinforces, doesn't resolve, the 2026-07-11 three-sub-shape flag above: whether a persona needs its own distinct view/type over the agent's-own-operational-identity sub-shape, not just the owner-data one, only makes sense to answer once that three-way split itself is decided, parked for this seam's real walk, not resolved here |
| n | New machine → owner's set | MSI | Open | Admission (hardware attestation is one admissible mechanism) and later revocation of a machine's trust. Prior art: Tailscale/WireGuard machine-key identity as the likely reference mechanism (kept out of anatomy.md the way vLLM is just an example; posture stays MSI, it's not an open protocol the way MCP is). Overlaps a's root-of-trust question: machine #1 and credential #1 are the same bootstrap moment. Finding, 2026-07-12 (Fleet manager piece pass): Signal's linked-device model (a primary device is mandatory, others join by scanning its QR code, owner-authenticated admission, no third party approves; revocation induces a key ratchet and a newly-linked device does not inherit past history) and Tailscale's device approval (an admin approves a new node; deauthorizing vs. deleting are two distinct, differently-final actions; node-key revocation is near-instant) are real precedent for the admission/revocation mechanics. Two open questions for the real walk: (1) who authorizes a new machine's admission, the owner acting from an already-trusted device (Signal's model) vs. some other attestation flow; (2) whether an admitted machine receives the full data immediately or a scoped subset, Signal's withheld-history precedent showing admission and revocation aren't symmetric operations. Seams k and n together are also the cross-implementation-fleet interoperability contract, real stakes for a future Collective/Armada made of independently-built fleets (flagged in ideas.md, not pursued now). Cross-referenced from p (2026-07-12): whether broker custody should be identity-scoped rather than machine-scoped in a fleet is an open question for this seam too, not just k's |
| p | Broker ↔ anything needing a secret | MSI | Open | New row, 2026-07-11: the old plan never asked this anywhere. How secrets are stored, scoped, and injected into exactly the call that needs them without entering model context; custody, not permission (that's j). Finding, 2026-07-12 (Credential broker piece pass): prior art splits into two genuinely distinct mechanisms, not one. Scoping/expiry of what the secret even is: HashiCorp Vault's dynamic secrets (a fresh, uniquely-scoped credential minted per lease, auto-revoked on expiry) and AWS STS `AssumeRole` (a scoped, short-lived credential triple, unconditional expiry) are the real precedent, but both still hand the live secret back to the caller as a normal return value, the caller holds real plaintext for the credential's lifetime. Keeping the plaintext out of the consumer's hands at all, even for a secret that exists: Claude Code's own sandbox proxy and OpenClaw's sentinel mechanism (confirmed against OpenClaw's own docs and repo) independently converge on the same shape, the consumer only ever sees an opaque sentinel, a component outside the consumer's own memory swaps in the real value only at the network/adapter boundary right as the call leaves; OpenClaw's docs honestly flag theirs as same-process protection only (the real value briefly exists in the same process's memory), a real, weaker guarantee than Claude Code's cross-process sandbox boundary. Letta adds a third variant, redaction in both directions ($SECRET_NAME substituted in, scrubbed from tool output on the way back), with secrets bound to agent identity rather than machine, a real cross-machine custody question for k/n. Confirmed negative precedent: Linux Secret Service API / gnome-keyring, macOS Keychain Services, sops/age, and `pass` all hand plaintext straight back to the requester, no injection concept, encrypted-at-rest only, useful as the baseline the broker has to go beyond, not as evidence for its injection claim. systemd-creds is a real middle case worth citing as a backend building block (kernel-enforced per-access check, not propagated to child processes the way env vars are, auto-cleaned on service exit) but still no per-call scoping, never the contract itself, same role Landlock/Capsicum play for seam q. Real digital-counterpart negatives, matching the project's running pattern: Hermes keeps secrets as plaintext in `~/.hermes/.env` with full-environment inheritance by every subprocess, a self-admitted gap on its own tracker; Khoj has no broker concept at all, plain env vars, consistent with its total absence of every other system-control piece found in earlier passes. Two open questions for the real walk: (1) whether injection needs a declarable strength tier (same-process sentinel-swap vs. cross-process boundary-swap), the same shape as seam q's already-open isolation-strength question; (2) whether Letta's identity-bound-not-machine-bound secrets mean broker custody should be identity-scoped rather than machine-scoped in a fleet |
| q | Kernel → sandbox | MSI | Open | Where an allowed action executes and what it can see while running; placement, not permission. Prior art: containers/isolated processes; WASI instantiation-time handle-passing; the per-OS sandbox family (Landlock, Capsicum, macOS sandbox) as hardening backends a placement can use, never the contract; another of the owner's machines or a remote service as placements, which ties into k. Finding, 2026-07-13 (Sandbox piece pass, 1.7): real prior art checked across two groups. General-purpose isolation: Claude Code's own sandbox (Seatbelt on macOS, bubblewrap+socat on Linux) is the most load-bearing, filesystem default-deny except working dir plus session temp, network is a domain allowlist enforced by a proxy, credentials injected as sentinels inside the sandbox and swapped for real values only at the proxy boundary when a request leaves for an allowlisted host, a real working answer for how q and the credential broker (p) interact without a secret ever entering the sandboxed process's own view; also confirms placements don't nest (Docker fails inside it). E2B runs everything in always-remote Firecracker microVMs addressed identically to a local placement, confirming "a remote service" as a real, common placement, not hypothetical. WASI's capability-handle model (no ambient `open()`, only `openat()` against handles passed at instantiation) confirms visibility is granted per-placement, not ambient, and is itself a real instance of this session's general roster-plus-local-check mechanism (see j's row). Landlock (unprivileged, self-applied, default-deny ruleset, newer ABI versions extend the same ruleset shape to network ports) confirms the per-OS hardening family (Landlock, Capsicum, macOS Seatbelt) are real backends a placement can build on, never the MSI's own contract, matching this row's existing framing. gVisor (userspace kernel intercepting syscalls, shared host kernel) versus Firecracker (own guest kernel via KVM) versus a container (runc/Docker) versus process isolation (seccomp+chroot+namespaces) is a real strength ladder, not the binary "container or isolated process" a flat category list implies. Digital-counterpart-specific: OpenClaw has the richest real precedent, sandboxing is opt-in and off by default, three independent settings (mode/scope/backend), Docker backend defaults `network: none`, `readOnlyRoot: true`, `capDrop: ALL`, and it ships a real SSH backend, a literal remote-machine placement with its own auth (host-key pinning) completely separate from the container's own isolation mechanism, confirming the tie to k is two distinct mechanisms (placement isolation, remote auth), not one; only tool execution is ever sandboxed, the agent's own reasoning stays on the host. Letta has no first-party sandbox, production guides point at interchangeable third-party providers (Modal, E2B, Daytona), real market confirmation placement is genuinely swappable, not framework-owned. Khoj has no code-execution isolation story at all, a real negative finding. Hermes tutorials use Docker as the entire isolation boundary, a volume-mounted directory as the whole visible filesystem, the simplest real tier. Open questions surfaced, not yet answered: (1) whether a placement's isolation strength (the microVM to userspace-kernel to container to process ladder above) should be a declarable, kernel/router-visible capability tag, the same shape as j's capability-tag finding, so a high-risk action can require "at least X" strength; (2) "what it can see" is really three separately-scoped things, filesystem, network, and secrets, every real system checked (Claude Code's sandbox, Landlock, WASI, OpenClaw) scopes them independently with default-deny on each, current anatomy prose treats this as one undifferentiated clause on purpose (Clarke's call, 2026-07-13, anatomy's level of detail is correct as-is, the split belongs here instead); (3) is "no sandbox, runs straight on the host" a legitimate placement MSI-1 must be able to express, OpenClaw defaults to it and Letta has nothing built in, real and common, not decided; (4) placements don't compose (Docker-in-Claude-Code's-own-sandbox fails), the contract shouldn't assume nesting |
| r | External content → a run | MSI | Open | Trust-tagging before content reaches the model: source, authorship, what vouches for it, so a stranger's words are never fed as if they were the owner's. The tag's on-disk fields are m's business; the tagging behavior and its single-instance-per-machine guarantee are this seam's. Finding, 2026-07-12 (Provenance piece pass): C2PA and Sigstore, both checked against their real specs, are genuine mismatches, media-authenticity-at-creation and software-supply-chain attestation respectively, not content-trust-for-an-agent; kept here only as structural precedent for "signed claim + transparency record," explicitly not as working mechanism for this seam. OpenFang's Ed25519 signing also checked and confirmed as miscited, it signs capability manifests, that's seam j's finding, not this one. Real fits: Anthropic's own shipped tool-content containment (a classifier inspects tool output before it enters context); DeepMind's CaMeL (2025), a taint-tracking interpreter tagging data with origin at the point it enters the system and gating at sensitive sinks, the load-bearing precedent, tag-here/enforce-at-kernel as two cooperating pieces, matching this seam's own split with j; OpenAI's Instruction Hierarchy (shipped as message-role privilege), confirmed real but insufficient alone, coarse role can't distinguish a web result from the owner's own file arriving under the same role. OpenClaw issue #7707 ("Memory Trust Tagging by Source," real, open, unimplemented) names this seam's exact shape, source-tiered trust plus decay, without having shipped it, the recognized-but-unbuilt pattern this project keeps finding. Hermes/Letta/Khoj: no mechanism found. Clarke's scope call (2026-07-12): MSI-1's mandatory core is origin-based tagging at the point content crosses in (mechanical, tied to fetch/tool path), never a mandated content classifier; active inspection is a legitimate richer tier implementations compete on, not part of the minimal core. Two open questions for this seam's real walk (step 2.14), parked not resolved: (1) whether tagging strength (mechanical origin-only vs. active content inspection) needs a declarable tier, the same shape as seam q's isolation-strength and seam p's injection-strength open questions; (2) whether a decay/reassessment policy belongs in the seam's mandatory contract or is owner-policy data, outside MSI-1's core either way per Clarke's minimal-core call |
| s | Harness ↔ memory provider | MSI | Open | New seam, minted 2026-07-11 (see the resolved flag above the Pieces table). What a run asks of a provider and what every compliant provider must accept and return: query in, sourced results out, so a harness can talk to whichever provider it is handed. Live candidate answer to check first: collapse into f, providers are just MCP servers exposing retrieval tools; if MCP's surface can't carry what retrieval needs (result provenance for r, view scoping cut by m's travelling permissions), it's an MSI envelope instead. Retrieval internals (how a provider indexes, embeds, ranks) stay deliberately open, providers compete there; whatever path a write takes, it lands in the data's shape, which is m's business. Finding, 2026-07-13 (Memory provider piece pass): checked two real POSIX precedents for the uniform-envelope-plus-free-inner-content shape. `ioctl(fd, request, arg)` is the closest literal match (fixed outer call, request/arg meaning left entirely to the driver) but is widely regarded as a design wart precisely because the freeform inner content lets every driver invent its own undocumented mini-protocol, quietly defeating the interoperability the uniform call appears to promise, the exact failure mode this seam exists to prevent. The VFS operation-table pattern (ext4/NFS/tmpfs all implementing the same fixed set of typed operations — open, read, write, readdir, stat — with freedom underneath each one, not inside one opaque call) is the healthier precedent and the current lean for s's real walk: a small fixed set of typed operations (e.g. search/get/write) each with a real defined request/response shape, provenance and permission-scoping mandatory on all of them, rather than one MCP-tool-name free-for-all. Not yet Decided, a lean for 2.7. Finding, 2026-07-13 (Sandbox piece pass, cross-cutting): the roster mechanism generalized this session reinforces, not changes, the Memory provider piece pass's existing plurality finding, several providers already serve one identity at once; this session just names the general mechanism (per-action resolution through j) that makes picking among them at run time principled rather than ad hoc |

## Precedent library

Trimmed to a pointer list; the load-bearing caveats now live in the seam rows
above. Depth per system stays in the devlog and gets re-earned when a seam is
walked.

- **Unix/POSIX** — runtime target, default vocabulary, and the method itself.
  Feeds a, i, j, m.
- **seL4** — capabilities, CDT + Revoke, no ambient authority. Feeds j.
- **Plan 9** — per-process namespaces, 9P, remote-through-local addressing.
  Feeds k, m.
- **Erlang/OTP** — cheap isolated processes, supervision, let-it-crash. Feeds
  i, k.
- **Capsicum / WASI / Landlock** — capability discipline on real userspace
  systems; WASI is the portable one. Feed j, q.
- **UCAN / Biscuit / macaroons / Cedar** — portable, attenuable authority as
  data. Feed j, m.
- **git object model / Venti** — immutable history, retraction vs. purge,
  branching. Feed m, k.
- **CRDTs (Automerge, Yjs) / Syncthing** — convergence without coordination.
  Feed k.
- **Matrix / XMPP** — one identity, many simultaneous clients. Feed c, b.
- **C2PA / Sigstore** — shipping provenance standards, checked 2026-07-12 against
  seam r's actual problem (is fetched content trustworthy to an agent) and
  found a real subject-matter mismatch (media authenticity at creation,
  software-supply-chain attestation, neither is content trust for an agent);
  kept as structural precedent for signed-claim-plus-transparency-record only.
  Feeds r (structurally).
- **CaMeL (DeepMind, 2025) / Anthropic's tool-content containment** — the real
  working precedent for seam r: origin-tagged data (CaMeL's taint tracking) or
  classifier-inspected tool output (Anthropic's shipped mechanism), gated at
  the point of use. Feeds r, j.
- **cron / systemd timers / RRULE** — trigger grammar. Feed g.
- **Agent-era literature** (AIOS, AgenticOS, Agent libOS, OpenFang) —
  contemporaneous and unproven, mechanism ideas only, never hardened
  precedent, except OpenFang: a real, verified implementation (real repo,
  real docs) bundling contract-issuance, enforcement, and budget/placement
  metering under a literal `openfang-kernel` component, independently
  convergent with this piece's own shape; still young, but a genuine
  candidate for scaffolding the kernel's first reference implementation, not
  just a lead. kernel.chat checked 2026-07-14 and dropped: no such product
  found. Feed d, i, j.
- **Closed comparators** (Alexa+, ChatGPT, Apple Intelligence, Meta AI) — what
  welded-shut looks like, checked 2026-07-10. Feed c, f (repointed 2026-07-12
  from the retired peripheral seam, o, to tools, its real successor).

### Explicitly excluded — inherited from the real OS underneath

Memory management, device-driver internals, the raw networking stack
(Tailscale/WireGuard), boot/init. Listed so the tracker stays honestly
complete, not because they need Mojo-side design.

## Naming

The formal spec is the **Mojo System Interface**, a direct echo of POSIX's own
full name (Portable Operating System Interface), not a claim of literal POSIX
compliance. Versions are numbered the same way: MSI-1 first, iterated openly.
Plain functional register per [naming-conventions.md](naming-conventions.md);
deliberately not "MojOS Standard," which would collide the abstract standard
with MojOS, the concrete NixOS product.

## Deliberately deferred

- §0 profile granularity: written at assembly from what landed; the ten
  named profiles are the working set, not a commitment. Whether Tools
  graduates from no profile to a named profile of its own is a live
  question, hinging on what seam f's walk (step 2.2) finds MSI has to add on
  top of raw MCP.
- Backup / disaster recovery: the all-machines-lost case. The shape being
  plain files means export is copying, so it's operational, not a seam — but
  if walking m proves it needs a defined export envelope, it gets a row then.
- Groups / Collectives: gated past MSI-1 per the sequencing discipline; the
  "N holders of one capability" question is noted at seam a for when it
  ungates.
- Implementation (repos, services, SDKs): blocked on the system being
  defined, not on this file.
